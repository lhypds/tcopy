txtcopier
=========


Setup
-----

Use 
python -m venv .venv
to create python virtual environment.

Use
.venv\Scripts\activate
to enable the envrionment.

Then run
pip install -r requirements.txt
